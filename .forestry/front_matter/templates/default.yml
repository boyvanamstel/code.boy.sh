---
label: Default
hide_body: false
fields:
- type: select
  name: layout
  config:
    source:
      type: simple
    required: true
    options:
    - post
  label: Layout
  default: post
- type: text
  name: title
  label: Title
  config:
    required: true
- type: select
  name: tags
  config:
    source:
      type: documents
      section: jekyll-posts
  label: Tags
  default: ''
- type: datetime
  name: date
  label: Date
pages:
- _posts/2019-04-02-my-current-take-on-the-menu-bar-icon-size.md
